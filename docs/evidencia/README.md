# Evidencia de testing

Screenshots tomados por el comando `/diseñar` (skill playwright-cli) como evidencia de que cada pantalla funciona.

- Nombre: `<pantalla>.png` (ej: `dashboard.png`).
- Se commitean junto con la pantalla.
